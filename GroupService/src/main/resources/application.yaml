server:
  port: 8086

spring:
  application:
    name: GroupService

  datasource:
    url: jdbc:postgresql://localhost:5432/groupdb
    username: postgres
    password: 13102002

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"